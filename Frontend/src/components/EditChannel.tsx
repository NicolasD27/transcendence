import React, { Fragment } from "react";
import './EditChannel.css';

interface Props {
	id: number;
}

const EditChannel: React.FC<Props> = (props) => {

	return (
		<div className="boxCard">
		</div >
	);
};

export default EditChannel;